{
	"name": "daemon-mcp",
	"version": "0.2.0-alpha",
	"description": "MCP server for the Daemon ecosystem - AI-queryable personal API with plugin support",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"format": "biome format --write",
		"lint:fix": "biome lint --fix",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
		"type-check": "tsc --noEmit"
	},
	"dependencies": {
		"agents": "^0.3.1",
		"zod": "^4.2.1"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.10",
		"@types/node": "^25.0.3",
		"typescript": "5.9.3",
		"wrangler": "^4.58.0"
	}
}
